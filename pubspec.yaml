name: appsi
description: A new Flutter project for Supabase and Dokploy.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0
  flutter_dotenv: ^5.1.0
  image_picker: ^1.0.7
  google_fonts: ^6.2.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  flutter_web_plugins: 
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env
