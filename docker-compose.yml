version: '3.8'

services:
  app:
    build:
      context: .
      args:
        - SUPABASE_URL=${SUPABASE_URL}
        - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
    ports:
      - "8080:80"
    restart: always
